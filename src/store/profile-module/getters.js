export function getProfiles(state) {
  return state.profiles;
}
export function getCurrentProfileId(state) {
  return state.current_profile_id;
}
export function getCurrentProfile(state) {
  return state.current_profile;
}

export function getFoundProfile(state) {
  return state.found_profile;
}
